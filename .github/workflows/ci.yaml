name: secrets workflow test

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v2

      - name: print text  
        run: echo "checked out code"

      - name: print the secret
        run: echo "the secret is ${{secrets.MY_SECRET}}"

